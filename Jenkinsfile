pipeline {
    agent any

    stages {
        stage('Run test file') {
            steps {
                bat "py --version"
                bat "python --version"        
                bat "python3 --version"
            }
        }
    }
}
